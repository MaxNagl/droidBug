apply plugin: 'java'

sourceCompatibility = 1.7
targetCompatibility = 1.7

sourceSets {
    main {
        resources {
            srcDirs '../files'
        }
    }
}
dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
}